version: '3'

# =============================================================================
# CUSTOMIZE THESE FOR YOUR PROJECT
# =============================================================================
vars:
  PROJECT_NAME: eks-cluster
  AWS_REGION: us-west-1
  # Paths for static analysis (terraform validate runs on each)
  VALIDATE_PATHS: "modules/eks-cluster examples/vpc examples/eks"

  # Go test configuration
  TEST_DIR: test
  INTEGRATION_TEST_TIMEOUT: 55m

  # Tool versions
  TFLINT_VERSION: v0.50.3

# Export Taskfile vars as environment variables for CI scripts and Go tests
env:
  PROJECT_NAME: "{{.PROJECT_NAME}}"
  AWS_REGION: "{{.AWS_REGION}}"
  TF_VAR_aws_region: "{{.AWS_REGION}}"
# =============================================================================

tasks:
  default:
    desc: "Show available tasks"
    cmds:
      - task --list

  ##############################################################################
  # Static Analysis Tasks
  ##############################################################################

  fmt:
    desc: "Check Terraform formatting"
    cmds:
      - terraform fmt -check -recursive -diff

  fmt-fix:
    desc: "Fix Terraform formatting"
    cmds:
      - terraform fmt -recursive

  validate-tf:
    desc: "Validate Terraform configuration (use '-- <dir>' for a single directory)"
    cmds:
      - |
        if [ -n "{{.CLI_ARGS}}" ]; then
          terraform -chdir="{{.CLI_ARGS}}" init -input=false -backend=false
          terraform -chdir="{{.CLI_ARGS}}" validate
        else
          ./ci/validate.sh "{{.VALIDATE_PATHS}}"
        fi

  tflint-init:
    desc: "Initialize TFLint and download plugins"
    internal: true
    cmds:
      - tflint --init

  tflint:
    desc: "Run TFLint linter"
    deps:
      - tflint-init
    cmds:
      - tflint --recursive --format=compact

  security-scan:
    desc: "Run Trivy security scan (use '-- json' for JSON output to trivy-results.json)"
    cmds:
      - |
        if [ "{{.CLI_ARGS}}" = "json" ]; then
          trivy config . --format json --severity CRITICAL,HIGH,MEDIUM --output trivy-results.json --skip-dirs ".terraform" --skip-dirs "**/.terraform"
          echo "Results saved to trivy-results.json"
        else
          trivy config . --format table --severity CRITICAL,HIGH,MEDIUM --exit-code 1 --skip-dirs ".terraform" --skip-dirs "**/.terraform"
        fi

  lint:
    desc: "Run all linting and security checks"
    cmds:
      - task: fmt-fix
      - task: validate-tf
      - task: tflint
      - task: security-scan

  ##############################################################################
  # Unit Tests (Fast, No AWS)
  ##############################################################################

  test-unit:
    desc: "Run unit tests (validation, helpers - no AWS)"
    cmds:
      - cd {{.TEST_DIR}} && go test -v ./unit/...

  test-unit-coverage:
    desc: "Run unit tests with coverage report"
    cmds:
      - ./ci/test_unit.sh --coverage {{.TEST_DIR}}

  ##############################################################################
  # Integration Tests (Real AWS)
  ##############################################################################

  test-integration:
    desc: "Run integration tests (Go tests handle full infra lifecycle)"
    summary: |
      Go tests deploy VPC, discover EKS versions, deploy parallel EKS clusters,
      validate, and destroy everything via defer. Only AWS credentials required.
      Override the test pattern: task test-integration -- TestEksClusterVersionMatrix
    cmds:
      - ./ci/test_integration.sh {{if .CLI_ARGS}}-run {{.CLI_ARGS}}{{end}} -timeout {{.INTEGRATION_TEST_TIMEOUT}} {{.TEST_DIR}}


  ##############################################################################
  # Combined Test Suites
  ##############################################################################

  test:
    desc: "Run local tests (fmt + validate + lint + unit tests)"
    cmds:
      - task: fmt
      - task: validate-tf
      - task: lint
      - task: test-unit

  test-all:
    desc: "Run everything: lint + unit + integration (deploys real AWS resources)"
    prompt: "This will deploy real AWS resources. Continue?"
    cmds:
      - task: lint
      - task: test-unit
      - task: test-integration

  ##############################################################################
  # Initialization Tasks
  ##############################################################################

  init:
    desc: "Initialize all Terraform configurations"
    internal: true
    cmds:
      - ./ci/validate.sh --init-only "{{.VALIDATE_PATHS}}"

  deps:
    desc: "Download Go dependencies"
    internal: true
    cmds:
      - cd {{.TEST_DIR}} && go mod download
      - cd {{.TEST_DIR}} && go mod tidy

  setup:
    desc: "Setup development environment"
    cmds:
      - task: version-check
      - task: init
      - task: deps
      - task: tflint-init
      - git config --unset-all core.hooksPath || true
      - pre-commit install
      - pre-commit install --hook-type pre-push

  ##############################################################################
  # Cleanup Tasks
  ##############################################################################

  clean:
    desc: "Deep clean (Terraform state + Go cache + .tflint cache)"
    cmds:
      - ./scripts/clean.sh --all

  cleanup-fallback:
    desc: "Safety net: cloud-nuke cleanup scoped to Pipeline + RunID (used by CI after integration tests)"
    internal: true
    cmds:
      - ./ci/cleanup.sh fallback --region {{.AWS_REGION}} --project {{.PROJECT_NAME}} --run-id ${PIPELINE_RUN_ID:-unknown}

  cleanup-run:
    desc: "Clean orphaned resources from current/last run (Pipeline + RunID). Add '-- force' to delete."
    cmds:
      - ./ci/cleanup.sh run --region {{.AWS_REGION}} {{if eq .CLI_ARGS "force"}}--force{{end}}

  cleanup-project:
    desc: "Clean ALL resources for this project (any RunID). Add '-- force' to delete."
    cmds:
      - ./ci/cleanup.sh project --region {{.AWS_REGION}} --project {{.PROJECT_NAME}} {{if eq .CLI_ARGS "force"}}--force{{end}}

  ##############################################################################
  # Development Workflows
  ##############################################################################

  watch:
    desc: "Watch for changes and run lint on save"
    cmds:
      - echo "Watching Terraform files for changes..."
      - |
        while true; do
          inotifywait -e modify -r modules/ examples/ || true
          echo "---"
          echo "Changes detected, running lint..."
          task lint || true
          echo "---"
        done
    platforms: [linux]

  ci:
    desc: "Run CI pipeline locally (like GitHub Actions)"
    cmds:
      - task: fmt
      - task: validate-tf
      - task: tflint
      - task: security-scan
      - task: test-unit

  ##############################################################################
  # Debug Tasks
  ##############################################################################

  debug-tf:
    desc: "Enable Terraform debug logging"
    cmds:
      - echo "export TF_LOG=DEBUG"
      - echo "export TF_LOG_PATH=/tmp/terraform.log"
      - echo "task test-integration"

  debug-go:
    desc: "Run tests with verbose Go logging"
    cmds:
      - echo "Running integration tests with -v flag..."
      - cd {{.TEST_DIR}} && go test -v -timeout 40m ./integration/...

  version-check:
    desc: "Check required tool versions"
    internal: true
    cmds:
      - ./ci/version_check.sh

  ##############################################################################
  # Helper Tasks (used by CI for matrix generation)
  ##############################################################################

  print-validate-paths:
    desc: "Print validate paths (used by CI to generate matrix)"
    internal: true
    silent: true
    cmds:
      - echo "{{.VALIDATE_PATHS}}"
